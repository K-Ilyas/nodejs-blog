<!DOCTYPE html>
<html lang="en">
<%- include("../partials/head") %>

    <body>
        <%- include("../partials/nav") %>
            <div class="row  justify-content-center">
                <div class="col-lg-10 col-md-10">
                    <h1 class="mt-5 text-decoration-underline">All Blogs</h1>

                    <% if(blogs.length == 0 ) { %>
                        <h3 class="mt-4">There are no blogs to display ...</h3>


                        <% } else { %>
                            <div class="row  justify-content-center mt-4">

                                <% blogs.forEach(blog => {  %>
                                    <!-- <a href="/blog/<%= blog._id %>" class="list-group-item list-group-item-action flex-column align-items-start">
                                        <div class="d-flex w-100 justify-content-between">
                                            <h3 class="mb-1">
                                                <%= blog.title %>
                                            </h3>
                                        </div>
                                        <p class="mb-1">
                                            <%= blog.snippet %>
                                        </p>
                                        <small><%= blog.body.substring(0,50) + " ..." %></small>
                                    </a> -->
                                    <div class="col-lg mt-5">
                                        <div class="card" style="width: 18rem;">
                                            <img src="<%= blog.image %>" class="card-img-top" alt="...">
                                            <div class="card-body">
                                                <h5 class="card-title card-title-custom">
                                                    <%= blog.title %>
                                                </h5>
                                                <p class="card-text">
                                                    <%= blog.body.substring(0,50) + " ..." %>
                                                </p>
                                                <a href="/blog/<%= blog._id %>" class="btn btn-primary">Details</a>
                                            </div>
                                        </div>
                                    </div>
                                    <% }) %>
                            </div>
                </div>

                <% } %>

            </div>
            <%- include("../partials/footer") %>

                </div>

                </div>
    </body>

</html>